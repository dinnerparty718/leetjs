const drink = {
  color: 'brown',
  carbonated: true,
  sugar: 40
}

type Drink = [string, boolean, number];

const peppsi: Drink = ['browm', true, 40]
const sprite: Drink = ['clear', true, 40];
const tea: Drink = ['green', false, 0];


const carSpecs: [number, number] = [40, 3354] // there is no menaing fo the number

const carStates = {
  horsepower: 40,
  wieght: 3354
}